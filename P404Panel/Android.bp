android_app {

    name: "P404Panel",

    static_libs: [
        "VolumePluginCommon",
    ],

    resource_dirs: ["res"],

    certificate: "platform",
    optimize: {
        enabled: false,
        proguard_flags_files: ["proguard.flags"],
    },
    platform_apis: true,

    srcs: ["src/**/*.java"],

}
